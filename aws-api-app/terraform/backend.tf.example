# Example backend configuration for Terraform state management
# Copy this file to backend.tf and customize for your environment

# S3 Backend Configuration
# Uncomment and configure the following block if using S3 backend

# terraform {
#   backend "s3" {
#     bucket         = "your-terraform-state-bucket"
#     key            = "aws-api-app/terraform.tfstate"
#     region         = "us-east-1"
#     encrypt        = true
#     dynamodb_table = "terraform-state-locks"
#   }
# }

# Local Backend Configuration (for development)
# Uncomment the following block for local state storage

# terraform {
#   backend "local" {
#     path = "terraform.tfstate"
#   }
# }

# Remote Backend Configuration (Terraform Cloud)
# Uncomment and configure the following block if using Terraform Cloud

# terraform {
#   backend "remote" {
#     organization = "your-organization"
#     
#     workspaces {
#       name = "aws-api-app"
#     }
#   }
# }